# 2026-04-30 Task Log

## 15:00

### Cursor Prompt

- 左栏 Ask/Agent 与联网搜索刷新后保持；左右侧栏宽度刷新后保持；展开/收起也记录；最后要求记录修改并提交。

### 修改内容总结

- `src/lib/storage/const.ts`：新增 `BUILDER_AI_CHAT`、`BUILDER_PANEL_WIDTHS`（存宽度与侧栏展开状态）。
- `src/lib/storage/index.ts`：`getBuilderAiChatPrefs` / `setBuilderAiChatPrefs`；`TBuilderPanelPrefs` 与 `getBuilderPanelPrefs` / `setBuilderPanelPrefs` / `patchBuilderPanelPrefs`（解析时夹紧宽度并兼容旧 JSON）。
- `src/lib/builder-panel-sizes.ts`：左右栏 min/max/default 与 clamp，供页面与 storage 共用。
- `src/app/builder/[id]/_components/left-panel.tsx`：AI 模式与联网搜索合并为 `aiPrefs`，挂载后从 storage 恢复，用户变更时写回 localStorage。
- `src/app/builder/[id]/page.tsx`：`panelPrefs` 统一宽度与左右展开；挂载恢复；拖拽宽度防抖写入；卸载时若有未落盘定时器则 flush；顶栏切换展开时 `patchBuilderPanelPrefs`。
