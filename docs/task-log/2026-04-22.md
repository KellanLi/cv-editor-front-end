# 2026-04-22 Task Log

## 01:54

### Cursor Prompt

（1）依据 `src/apis/` 示例生成编写 API 的 skill 供过目；（2）确认后加入项目 skill（AGENTS.md）；（3）在 task-log 记录并提交。

### 修改内容总结

- **`.cursor/skills/apis-layer/SKILL.md`**：新建，约定 `get`/`post`、`src/types/api` 类型外置、命名与自检清单。
- **`AGENTS.md`**：在 Project context 中增加 **apis-layer** 技能路径说明。

## 02:25

### Cursor Prompt

记录本次修改到 task log 并提交 git。

### 修改内容总结

- **依据 `docs/api-docs-json.json`（OpenAPI）**：补全 **`src/apis/`**（`resume.ts`、`section.ts` 真实实现；`user.ts` 增加 `userProfile` GET；`auth.ts` 风格统一）、**`src/types/api`** 与各 **`src/types/business`** 实体；**`sections/page.tsx`** 列表改为调用 `content-template` API。
- **类型约定**：前端实体命名不使用 `*Dto`；领域实体放 **`types/business`**，列表 **`pagination`** 统一用 **`IPagination`**（`types/api/pagination.ts`），去掉各 list 内重复分页类型；**`.cursor/skills/apis-layer/SKILL.md`** 随约定更新。
- **`TUser` / `TToken` / `TInfoTemplate` / `TContentTemplate` / `TResume` / `TSection` / `TLoginData`**：按文档字段对齐；创建/更新模版请求用 **`TContentTemplateInfoLayerInput`**（`Pick` 信息层维度字段）。
- **`TInfo` / `TContent`**：与 **`InfoDto` / `ContentDto`** 字段一致，`values` 为 **`string[]`**；**`TSection.contents`** 为 **`TContent[]`**；**`section-render`**、**`build-section-preview-model`**、**`TUpdateSectionContentInfoReq.values`** 同步占位与类型。
