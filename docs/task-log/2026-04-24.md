# 2026-04-24 Task Log

## 02:45

### Cursor Prompt

1. 右侧栏「模块管理」列表项加删除按钮；2. 已加入的模块在「添加模块」弹窗中不能重复添加。

### 修改内容总结

**`src/app/builder/[id]/_components/section-list.tsx`**

- 新增 `remove` mutation（`section.remove`）与二次确认 `Modal`（状态 `deleteModal` + `deleteTarget` + `deleteError`）。
- 每行追加 `Trash2` 图标按钮（`Button variant="ghost" isIconOnly aria-label="删除模块「X」"`），放在名称右侧，点击仅触发 `onRequestDelete(section)` 打开弹窗，不参与 dnd；拖拽手柄仍独占 `listeners`。
- `handleConfirmDelete` 成功后 `invalidateQueries(resumeSectionsQueryKey(resumeId))`，由父级 query 驱动列表刷新；失败保留 Modal 并显示错误。
- 为在空列表时也能挂载 `Modal`，把「暂无模块」占位移入最外层 `div`，避免早期 `return` 把 Modal 一起省掉。

**`src/app/builder/[id]/_components/add-section-modal.tsx`**

- 新增 `sections: TSection[]` prop；以 `useMemo` 派生 `addedTemplateIds: Set<number>`（按 `contentTemplateId`）。
- 卡片渲染时：若 `added`，`button` 加 `disabled` / `aria-disabled` + `cursor-not-allowed opacity-60`，右上角用「已添加」胶囊代替原来的选中圆点；`handleToggle` 提前 return 防御性处理。
- `handleConfirm` 改为直接用 props 里的 `sections` 计算 `baseOrder`（父级 query 缓存在删除/排序后会同步），不再走 `queryClient.getQueryData`。

**`src/app/builder/[id]/_components/right-panel.tsx`**

- 将自身持有的 `sections` 继续透传给 `AddSectionModal` 的新 `sections` prop。

## 02:57

### Cursor Prompt

依据右侧模块列表，在中间简历编辑/预览区域用 `src/components/section-render.tsx` 按顺序渲染对应模块；自定义模块的 hover / edit / view 状态要与个人信息模块保持一致。

### 修改内容总结

**`src/app/builder/[id]/page.tsx`**

- 将 `content-template-lookup` 查询从 `RightPanel` 上移到 `page.tsx`，统一 `queryKey: ['content-template-lookup']`；在此处派生 `contentTemplateMap: Map<number, TContentTemplate>`，同时下传给 `CenterPanel` 与 `RightPanel`，避免两处重复请求 / 重复构表。
- `CenterPanel` 新增 `sections` / `sectionsPending` / `sectionsError` / `contentTemplateMap` props；`RightPanel` 也同步加上 `contentTemplateMap` prop。

**`src/app/builder/[id]/_components/right-panel.tsx`**

- 移除自身的 `useQuery` 与 `contentTemplateMap` 派生，`contentTemplateMap` 改由 props 接收；`Tabs` 结构与 `SectionList` 用法保持不变。

**`src/app/builder/[id]/_components/center-panel.tsx`**

- 扩展为先渲染 `ProfileModule`，再按 `sections`（已按 `order` 升序）map 出 `CustomSectionModule`；父级 `gap-6 flex-col` 直接控制间距。
- `resume/detail` pending/error 时仍走整体 loading/error 分支；`resume` 就绪后，模块区内再单独处理 `sectionsPending`（spinner）与 `sectionsError`（红字）。

**`src/app/builder/[id]/_components/custom-section-module.tsx`（新建）**

- 包裹 `SectionRender` 的自定义模块，视觉与交互与 `ProfileModule` 一致：
  - **View**：`cursor-pointer rounded-xl border-2 border-dashed border-transparent p-3 sm:p-4 hover:border-sky-400/75 hover:bg-sky-50/95 focus-visible:…`，支持 `onClick` / Enter / Space 进入编辑。
  - **Edit**：`rounded-xl border-2 border-dashed border-sky-400/75 bg-sky-50/95 p-3 sm:p-4`，顶部可显示 `text-danger` 的 `saveError`；内部 `SectionRender` 传 `status="edit"` 并驱动 `draftContents`。
- 状态管理：`moduleStatus: 'view' | 'edit'` + `draftContents: TContent[]`；进入 edit 时以 `sortContents(section.contents)` 重置草稿，view 时 `useEffect` 同步 props 更新。
- **退出编辑**：document 上 `mousedown` + 捕获阶段 `Escape` 监听；`rootRef.contains` 内部点击忽略，`closest('[role="dialog"], [data-slot="popover"]')` 放行浮层（DatePicker 等）。
- **保存**：`updateMutation = useMutation(updateContent)`；`toUpdatePayload` 把草稿重新编号为 `contents[i].order = i+1`、`infos[j].order = j+1`，提交 `{ sectionId, contents }`；成功后用返回的 `TSection` 走 `queryClient.setQueryData(resumeSectionsQueryKey, …)` 就地替换并重新按 `order` 排序，避免一次额外 `section/list` round trip。
- **模版未就绪容错**：`contentTemplate` 为 undefined 时不进入 edit（`enterEdit` 提前 return），渲染时回退到 `{ name: '加载中…', infoTemplates: [] }`，等父级 `contentTemplateMap` 就绪后重渲染即可。

## 14:00

### Cursor Prompt

记录本次修改并提交。

### 修改内容总结

- **`src/components/section-render.tsx`**：编辑态下「添加内容」去掉上分割线、改为 `pt-6`；信息层仅 **active** 时加 `p-3`，未选中更紧。
- **`src/components/info-layer/rich-text.tsx`**：`TextField` + `Label` + `inputVariants`（`@heroui/styles`）与单行 `Input` 视觉与焦点态对齐（含 `data-slot="input"`、hover / `focus-within` 与 `status-focused-field`）。
- **`src/app/builder/[id]/_components/center-panel.tsx`**：简历主列模块纵向间距由 `gap-6` 改为 `gap-4`。
- **同次提交一并纳入的未提交工作**：`page` / `right-panel` / `section-list` / `add-section-modal` / `custom-section-module` / `profile-module` 等 Builder 中间区与侧栏；`src/apis/section.ts` 与 `src/types/api/section/*`、`src/types/business/resume.ts`、`src/lib/builder-resume-keys.ts`；`title-and-time-period`；`docs/api-docs-json.json` 与 `docs/task-log/2026-04-23.md`。

