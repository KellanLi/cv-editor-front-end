# 2026-05-04 Task Log

## 15:41

### Cursor Prompt

- 根据 `docs/api-docs-json.json` 更新 JD 相关接口定义；在构建页左栏 AI 区增加 JD 按钮与 Modal（展示/编辑、保存调用接口）；去掉 Modal 中「内容形如 JSON」类说明；最后要求记录修改并提交。

### 修改内容总结

- `docs/api-docs-json.json`：同步后端 OpenAPI 快照（含 `resume/update-job-description`、`ResumeDto.jobDescriptionText`、`resume/stream-updates` 及若干 AI 相关描述调整）。
- `src/types/api/resume/update-job-description.ts`：新增 `TResumeUpdateJobDescriptionReq`。
- `src/types/business/resume.ts`：`TResume` 增加可选字段 `jobDescriptionText`。
- `src/apis/resume.ts`：新增 `updateJobDescription`（`POST /resume/update-job-description`）。
- `src/app/builder/[id]/_components/left-panel.tsx`：`useQuery(resumeQueryKey)` 与详情缓存复用；公文包按钮打开 JD Modal；`jobDescriptionToDraft` / `draftToJobDescriptionPayload`；保存后 `invalidateQueries`；说明文案仅保留「复用 / 清空」句。

## 15:50

### Cursor Prompt

中文输入法在 AI 对话 textarea 中用 Enter 上屏英文字母时被当成发送；修复后要求记录修改并提交。

### 修改内容总结

- `src/app/builder/[id]/_components/left-panel.tsx`：为输入框增加 IME 组合状态（`onCompositionStart` / `onCompositionEnd` + `imeComposingRef`，结束组合用 `setTimeout(0)` 延迟复位）；`Enter` 在组合中或组合刚结束时只跳过发送（不 `preventDefault`），避免与输入法选词上屏冲突。
