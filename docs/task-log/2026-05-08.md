# 2026-05-08 Task Log

## 04:47

### Cursor Prompt

请提交所有的修改并记录。

### 修改内容总结

- `next.config.ts`：开发代理增加 `experimental.proxyTimeout`（10 分钟），避免长耗时 AI 接口被默认 30s 断开。
- `src/lib/request.ts`：新增 `postSse`，带鉴权的 POST 流式请求（不解析 JSON），供 SSE 端点使用。
- `src/apis/ai.ts`：简历 AI 诊断同步 `diagnoseResume`；异步 `startResumeDiagnoseTask` / `getResumeDiagnoseTaskStatus`；`streamChat` 改为走 `postSse`。
- `src/types/api/ai/resume-diagnose*.ts`、`src/types/business/resume-diagnosis*.ts`、`resume-stream.ts`：诊断与简历流事件类型。
- `src/app/builder/[id]/_components/resume-diagnosis-panel.tsx`：诊断 UI（轮询任务、报告展示、本地 TTL 缓存）；`right-panel.tsx` 增加「诊断」Tab 与持久化选中项。
- `src/lib/storage/const.ts`、`index.ts`：`BUILDER_RESUME_DIAGNOSIS` 及按简历 ID 读写的诊断快照 API。
- `src/lib/resume/stream-updates.ts`、`use-resume-auto-refresh.ts`：订阅 `resume/stream-updates` SSE，收到更新后 invalidate 简历与列表查询。
- `center-panel.tsx`、`rich-text.tsx`、`info-layer-modal.tsx`：相关联动或小修正。
- `docs/api-docs-json.json`：OpenAPI 快照同步；新增 `docs/ai-design.md`、`docs/thesis-context-assets.md`；`assets/` 配图。
- `docs/task-log/2026-04-30.md`：补记此前会话条目。
