# 2026-04-13 Task Log

## 20:48

### Cursor Prompt

依据 task-log 技能记录本次修改并提交 git；此前会话涉及：富文本信息层（Tiptap 工具栏、段落缩进扩展、布局与报错修复）、`@tiptap/core` 类型声明、标题+时间段展示样式与 INFO_LAYER 示例数据等。

### 修改内容总结

- **富文本（`rich-text.tsx`）**：`active` 切换编辑/只读；`values[0]` 存 HTML；`onUpdate` 回写；只读用 `useEditor({ editable: false })`。
- **工具栏**：抽至 `info-layer/components/rich-text-toolbar.tsx`（撤销/重做、加粗/斜体、列表、段落缩进、清除格式）；与编辑区同宽、贴顶分隔布局。
- **段落缩进**：新增 `tiptap-paragraph-indent.ts`（`data-indent` + `increaseParagraphIndent` / `decreaseParagraphIndent`）；修复链式命令下误用 `state.selection` 的报错；`Commands` 类型按 `paragraphIndent` 分组以支持 `can()` / `chain()`。
- **依赖**：直接依赖 `@tiptap/core`（模块合并与类型）。
- **标题 + 时间段**：展示态标题与日期区间使用 `text-lg font-bold`。
- **示例数据**：`const.ts` 中三类信息层 defaultProps 填入示例文案/日期/富文本 HTML；信息层弹窗 `Modal.Container` 去掉固定 `size="lg"`。
