# 2026-04-27 Task Log

## 16:01

### Cursor Prompt

- `left-panel.tsx` 的 Ask/Agent 下拉与 `chat-send.ts` 的 `purpose` 对齐，在发送请求时补充 `purpose` 字段。
- 排查并修复「新建对话首条消息返回 `对话无可用历史消息`」的问题。
- 记录本次修改并提交。

### 修改内容总结

- `src/app/builder/[id]/_components/left-panel.tsx`：新增 `MODE_TO_PURPOSE` 映射（`ask -> BASIC_QA`、`agent -> DIALOGUE_EDIT`），发送流式请求时按当前下拉模式传 `purpose`。
- `src/app/builder/[id]/_components/left-panel.tsx`：`upsertStreamCreatedConversationInCache` 改为接收并写入本次请求的 `purpose`，不再固定写死 `DIALOGUE_EDIT`。
- `src/app/builder/[id]/_components/left-panel.tsx`：模式默认值调整为 `agent`；新建对话时重置为 `agent`；切换已有会话时按会话 `purpose` 自动同步下拉模式，避免会话类型与请求 `purpose` 失配。
