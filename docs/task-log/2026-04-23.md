# 2026-04-23 Task Log

## 02:11

### Cursor Prompt

阅读 `docs/api-docs-json.json`，为新增的接口补齐 API 定义（最终确认 4 个路径全量覆盖）。

### 修改内容总结

- **`src/apis/resume.ts`**：新增 `updateTitle` / `updateProfile`，对应 `/resume/update-title`、`/resume/update-profile`，响应体复用 `TResume`。
- **`src/apis/storage.ts`**：新建，`upload(file)` 走 multipart/form-data 直接调用 `request`；附带 `fileUrl(storedName)` helper，拼接 `/api/v1/storage/file/{storedName}` 供 `<img>` 兜底使用。
- **`src/lib/request.ts`**：识别 `options.body instanceof FormData` 时跳过默认 `Content-Type: application/json`，交由浏览器生成 multipart 边界；其余行为不变。
- **`src/types/api/resume/update-title.ts`**：新增 `TResumeUpdateTitleReq`（`id` + `title`）。
- **`src/types/api/resume/update-profile.ts`**：新增 `TResumeUpdateProfileReq`（`id` 必填 + `TResumeProfile` 其余字段 Partial 覆盖）。
- **`src/types/business/resume-profile.ts`**：新增 `TResumeProfile`，字段对齐 `ResumeProfileTableDto`，`profileExtra` 按描述语义收敛为 `Record<string, string>`。
- **`src/types/business/resume.ts`**：`TResume` 增加可选 `profile: TResumeProfile`，与 `ResumeDto.profile` 对齐。
- **`src/types/business/uploaded-file.ts`**：新增 `TUploadedFile`，字段对齐 `UploadStorageDataDto`。
- **`docs/api-docs-json.json`**：同步最新后端快照（含新增 storage、resume update 相关 path 与 schema）。

## 12:10

### Cursor Prompt

依据 `docs/resume-editor-page-design.md`：在简历列表页加「创建简历」入口并跳转到编辑页；在编辑页上完成顶部导航栏与三栏布局。后续追加：左右栏可拖动调整宽度；修复 `Separator--vertical` 的 `align-self: stretch` 对齐问题、去掉面板/Tabs 的底边框、为中间编辑预览区添加圆角；把圆角实际挂到 `<main>`、Tabs 上方留一点 margin；为外层容器添加符合 HeroUI 设计规范的背景色。

### 修改内容总结

**简历列表页（`src/app/dashboard/resumes/`）**

- **`page.tsx`**：替换原占位实现，参照 `sections/page.tsx` 结构：`SearchField`（按 `title` 过滤） + 右上 `ResumeCreator` + `Card` 网格（两列 / xl 三列） + `Pagination`，含 loading / error / empty 三态；`useQuery` key 为 `['resume-list', page, pageSize, title]`。
- **`_components/resume-creator.tsx`**：新建。`Modal` + `TextField` 输入简历名称，走 `/resume/create`；成功后 `invalidateQueries(['resume-list'])` 并 `router.push('/builder/{id}')`。
- **`_components/resume-card.tsx`**：新建。占位 `FileText` 图标 + 标题 + `updatedAt`；`role="button"` + 键盘 Enter/Space 跳转编辑页；右上 `Popover` 含删除项，删除走二次确认 `Modal`。

**简历编辑页（`src/app/builder/[id]/`）**

- **`layout.tsx`**：新建。为 `/builder/*` 路由挂 `QueryClientProvider` + `UserStoreProvider`（dashboard 之外没有这层 Provider）。
- **`page.tsx`**：新建。顶部 `BuilderTopBar` + 下方 flex 三栏；左右栏用显式宽度状态（`leftWidth` / `rightWidth`）渲染 + `ResizeHandle` 替代原 `Separator`；左栏范围 `240~520`，右栏 `280~520`，均默认 `320`；收起/展开只影响条件渲染，宽度状态保留。外层容器使用 `bg-background`；`<main>` 使用 `bg-default-50 rounded-2xl overflow-hidden`，四角会露出外壳色形成"悬浮画布"观感。
- **`_components/builder-top-bar.tsx`**：新建。左侧：返回按钮（`Tooltip` + `router.push('/dashboard/resumes')`）、简历名称、状态徽章（`同步中` / `已保存`）；右侧：左右栏 toggle、导出、保存；icon-only 按钮统一 `Tooltip`；不使用 `Separator`（其 `align-self: stretch` 会强制撑满行高），改为内部 `InlineDivider` 组件（`bg-default-200 h-5 w-px self-center`）。顶栏去掉底边框，由内容区 `bg-default-50` 与顶栏 `bg-background` 自然区分层次。
- **`_components/left-panel.tsx`**：新建。`AI 对话` 占位区，去掉 header 的底边框。
- **`_components/center-panel.tsx`**：新建。A4 比例白纸卡片 + 个人信息模块占位区；背景色和圆角由外层 `<main>` 负责，自身只承担内容布局。
- **`_components/right-panel.tsx`**：新建。HeroUI `Tabs`（默认 primary 变体）「模块管理 / 设计 / AI 诊断」，`Tabs.ListContainer` 加 `mt-2` 与面板顶拉开；不再手动加底边框，沿用 HeroUI 自带的 `Tabs.Indicator`。
- **`_components/resize-handle.tsx`**：新建。垂直方向拖拽手柄：1px 视觉线 + 左右各 4px 透明热区；`onPointerDown` 记录起点、`setPointerCapture` 接管后续事件；通过 `computeWidth(dx, base)` 让父级决定左/右栏方向；`aria-orientation="vertical"` + `role="separator"` + `aria-valuenow/min/max`；键盘 `←/→` 细调（默认 16px 步长），`Home/End` 贴最小/最大值；拖拽中加 `data-dragging` 切换 accent 色。

## 12:20

### Cursor Prompt

`@docs/api-docs-json.json` 同步新增的接口。

### 修改内容总结

- **`docs/api-docs-json.json`**：对比 diff，后端新增 `POST /resume/detail`（`DetailResumeDto` → `ResumeDto`，简历详情含 ResumeProfile），其余路径/schema 未变。
- **`src/types/api/resume/detail.ts`**：新建，导出 `TResumeDetailReq = { id: number }`。
- **`src/apis/resume.ts`**：新增 `detail(params)` 薄封装，复用 `TResume` 作为响应 `data` 类型，与 `create/updateTitle/updateProfile` 对齐。

## 22:21

### Cursor Prompt

基于 `docs/resume-editor-page-design.md` 与 `resume/detail` 接口：完成顶栏的简历标题加载态；实现个人信息模块的 view/edit 两态，含证件照上传弹窗、字段编辑、自定义标签；按设计稿反复调整样式，使用 `@internationalized/date` 的 `DatePicker` 替代原生 `<Input type="date">`；支持"点击模块外"隐式保存退出；为输入框加入清空按钮，并把空值以 `null` 提交给后端。

### 修改内容总结

**简历编辑页路由层（`src/app/builder/[id]/`）**

- **`page.tsx`**：引入 `useParams` + `useQuery`（`['resume', resumeId]`）调用 `resume.detail`；校验 `id` 非正整数时直接走 error 态。把 `resume` / `isPending` / `isError` / `resumeError` 透传给 `CenterPanel`，把标题 + `titleState` 透传给 `BuilderTopBar`，切换简历时用 `key={resumeId}` 重置子组件状态。
- **`_components/builder-top-bar.tsx`**：新增 `TTitleState = 'loading' | 'error' | 'ready'` 与同名可选 prop；`loading` 用 `animate-pulse` 骨架条、`error` 显示「无法加载简历」并隐藏状态徽标；`ready` 走原来的标题+同步/已保存徽标逻辑。
- **`_components/center-panel.tsx`**：接入 `resumeId/resume/isPending/isError/error`，分别渲染 `EmptyState`（无效 id）/ `Spinner`（加载中）/ `text-danger`（接口错）/ `ProfileModule`（就绪）。

**个人信息模块（`src/app/builder/[id]/_components/profile-module.tsx`，新建）**

- **状态与布局**：`'view' | 'edit'` 切换；view 点击 / Enter / Space 进入编辑；进入编辑后 `document` 上挂 `mousedown` + `Escape` 监听，点到模块外或按 Esc 触发「完成」（调用 `updateProfile` 并切回 view）；`closest('[role="dialog"], [data-slot="popover"]')` 用于放行 Modal / Calendar 等 portal 浮层，`photoDialog.isOpen` 期间完全不触发。
- **View 模式**：去除边框与"个人信息"标题；左上姓名 `text-2xl sm:text-3xl font-bold`；其余字段（出生日期/性别/目标岗位/邮箱/电话）放同一浅灰块内，`flex-wrap` + `Calendar/UserRound/Target/Mail/Phone` 图标；自定义标签另起一块；证件照固定右上，尺寸 `w-24 h-32`。Hover/`focus-visible` 复用 edit 的 `border-sky-400/75 border-2 border-dashed + bg-sky-50/95`，切换无抖动。
- **Edit 模式**：外层 `rounded-xl bg-sky-50/95 border-2 border-dashed border-sky-400/75 p-3 sm:p-4`；字段网格 `sm:grid-cols-2`，`Label text-xs` + `InputGroup h-9 min-h-0 text-sm`；证件照按钮位于右侧 `self-end/self-start`，点击打开上传 Modal（本地预览 `URL.createObjectURL` → `upload(file)` → `updateProfile({ id, photoUrl })` → 关 Modal、释放 blob）。
- **日期字段**：参照 `components/info-layer/title-and-time-period.tsx` 改用 HeroUI `DatePicker + DateField.Group + Calendar` 组合；新增 `parseCalendarDateSafe`（空串/非法返回 `null`，避免 `parseDate('')` 抛错）；存储格式仍为 `YYYY-MM-DD`，对外 `dateInputToIso` → ISO UTC 字符串；lucide `Calendar` 与 HeroUI `Calendar` 同名冲突，前者 import 为 `CalendarIcon`。
- **可清空输入**：新建 `ClearableTextField`（文件内组件），`TextField + InputGroup`，值非空时 Suffix 显示 `X` 按钮，点击 `onChange('')`；5 个文本字段（姓名 / 性别 / 目标岗位 / 联系邮箱 / 联系电话）统一替换。DatePicker 的 `DateField.Suffix` 也加了对称的清空按钮，`onPress` 设 `birthDate: ''`。
- **置空语义**：`toUpdatePayload` 引入 `emptyToNull`，所有空字符串 / 空日期 / 空数组统一输出 `null`，表示显式清空；非空才发原值；`id` 始终必填。
- **图片警告**：为 3 处 `<img>`（view 证件照、edit 预览、Modal 本地预览）加上 `{/* eslint-disable-next-line @next/next/no-img-element */}`，不引入 `next/image`（blob: URL 需要 `unoptimized`，且这是小尺寸非 LCP 头像）。

**类型 / 契约**

- **`src/types/business/resume-profile.ts`**：`TResumeProfile` 增加可选 `gender`（设计稿必填字段，后端字段名未约定前先在前端落地）。
- **`src/types/api/resume/update-profile.ts`**：引入自定义 `TNullable<T>` 泛型，改写 `TResumeUpdateProfileReq` 为 `{ id: number } & TNullable<Omit<TResumeProfile, 'id' | 'resumeId'>>`；`undefined` 表示「不变更」，`null` 表示「显式清空」。

## 22:33

### Cursor Prompt

基于 `docs/resume-editor-page-design.md` 的「多功能区 / 模块管理」设计：先做模块管理。点击「添加模块」打开类似 `dashboard/sections/` 的列表弹窗，支持分页多选；添加后在模块列表中展示模块名称并可拖拽排序。

### 修改内容总结

**`src/app/builder/[id]/_components/add-section-modal.tsx`（新建）**

- 触发按钮 + 受控 `Modal`（`max-w-4xl`），参照 `dashboard/sections/page.tsx` 的 `SearchField` + 分页骨架；`useQuery` key `['content-template-list', page, pageSize, deferredName]`，`enabled: overlay.isOpen` 避免未打开就请求。
- 跨分页多选：`selected: Map<number, TContentTemplate>` 持有完整 item，切换分页保留选择；卡片为自定义 `button[aria-pressed]` + 右上选中态圆形 `Check` 指示器，选中时 `border-accent/60 bg-accent/10`。
- 确认时串行 `section.create({ resumeId, contentTemplateId })`（稳定追加顺序），全部成功后 `invalidateQueries(['resume', resumeId])`；失败态 footer 显示错误；取消按钮走 `slot="close"`，打开时 `resetState()`。

**`src/app/builder/[id]/_components/section-list.tsx`（新建）**

- 参照 `dashboard/sections/_components/info-layer-field-list.tsx` 搭 `@dnd-kit` 垂直排序：`PointerSensor(distance: 8)` + `KeyboardSensor` + `DragOverlay`；行内显示 `GripVertical` 拖拽手柄 + 模块名（`contentTemplateMap.get(contentTemplateId)?.name ?? '未命名模块'`）。
- 拖拽后用 `queryClient.setQueryData<TResume>(['resume', resumeId], ...)` 直接改 `sections` 顺序；加 TODO 注释：后端暂无模块排序接口，刷新后顺序会回滚到后端返回值。

**`src/app/builder/[id]/_components/right-panel.tsx`**

- 新增 props `{ resumeId: number | null; sections: TSection[] }`；`useQuery(['content-template-lookup'])` 拉一页 `pageSize=200` 的 ContentTemplate 用于 id → 名称查找（缓存跨 tab 复用）。
- 「模块管理」面板内渲染 `AddSectionModal`（`variant="secondary"` + `w-full justify-center gap-2`）与 `SectionList`；`resumeId == null` 时降级为占位提示。

**`src/app/builder/[id]/page.tsx`**

- 把 `resumeId` 与 `resume?.sections ?? []` 透传给 `RightPanel`。
