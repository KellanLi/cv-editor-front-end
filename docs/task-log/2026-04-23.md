# 2026-04-23 Task Log

## 02:11

### Cursor Prompt

阅读 `docs/api-docs-json.json`，为新增的接口补齐 API 定义（最终确认 4 个路径全量覆盖）。

### 修改内容总结

- **`src/apis/resume.ts`**：新增 `updateTitle` / `updateProfile`，对应 `/resume/update-title`、`/resume/update-profile`，响应体复用 `TResume`。
- **`src/apis/storage.ts`**：新建，`upload(file)` 走 multipart/form-data 直接调用 `request`；附带 `fileUrl(storedName)` helper，拼接 `/api/v1/storage/file/{storedName}` 供 `<img>` 兜底使用。
- **`src/lib/request.ts`**：识别 `options.body instanceof FormData` 时跳过默认 `Content-Type: application/json`，交由浏览器生成 multipart 边界；其余行为不变。
- **`src/types/api/resume/update-title.ts`**：新增 `TResumeUpdateTitleReq`（`id` + `title`）。
- **`src/types/api/resume/update-profile.ts`**：新增 `TResumeUpdateProfileReq`（`id` 必填 + `TResumeProfile` 其余字段 Partial 覆盖）。
- **`src/types/business/resume-profile.ts`**：新增 `TResumeProfile`，字段对齐 `ResumeProfileTableDto`，`profileExtra` 按描述语义收敛为 `Record<string, string>`。
- **`src/types/business/resume.ts`**：`TResume` 增加可选 `profile: TResumeProfile`，与 `ResumeDto.profile` 对齐。
- **`src/types/business/uploaded-file.ts`**：新增 `TUploadedFile`，字段对齐 `UploadStorageDataDto`。
- **`docs/api-docs-json.json`**：同步最新后端快照（含新增 storage、resume update 相关 path 与 schema）。

## 12:10

### Cursor Prompt

依据 `docs/resume-editor-page-design.md`：在简历列表页加「创建简历」入口并跳转到编辑页；在编辑页上完成顶部导航栏与三栏布局。后续追加：左右栏可拖动调整宽度；修复 `Separator--vertical` 的 `align-self: stretch` 对齐问题、去掉面板/Tabs 的底边框、为中间编辑预览区添加圆角；把圆角实际挂到 `<main>`、Tabs 上方留一点 margin；为外层容器添加符合 HeroUI 设计规范的背景色。

### 修改内容总结

**简历列表页（`src/app/dashboard/resumes/`）**

- **`page.tsx`**：替换原占位实现，参照 `sections/page.tsx` 结构：`SearchField`（按 `title` 过滤） + 右上 `ResumeCreator` + `Card` 网格（两列 / xl 三列） + `Pagination`，含 loading / error / empty 三态；`useQuery` key 为 `['resume-list', page, pageSize, title]`。
- **`_components/resume-creator.tsx`**：新建。`Modal` + `TextField` 输入简历名称，走 `/resume/create`；成功后 `invalidateQueries(['resume-list'])` 并 `router.push('/builder/{id}')`。
- **`_components/resume-card.tsx`**：新建。占位 `FileText` 图标 + 标题 + `updatedAt`；`role="button"` + 键盘 Enter/Space 跳转编辑页；右上 `Popover` 含删除项，删除走二次确认 `Modal`。

**简历编辑页（`src/app/builder/[id]/`）**

- **`layout.tsx`**：新建。为 `/builder/*` 路由挂 `QueryClientProvider` + `UserStoreProvider`（dashboard 之外没有这层 Provider）。
- **`page.tsx`**：新建。顶部 `BuilderTopBar` + 下方 flex 三栏；左右栏用显式宽度状态（`leftWidth` / `rightWidth`）渲染 + `ResizeHandle` 替代原 `Separator`；左栏范围 `240~520`，右栏 `280~520`，均默认 `320`；收起/展开只影响条件渲染，宽度状态保留。外层容器使用 `bg-background`；`<main>` 使用 `bg-default-50 rounded-2xl overflow-hidden`，四角会露出外壳色形成"悬浮画布"观感。
- **`_components/builder-top-bar.tsx`**：新建。左侧：返回按钮（`Tooltip` + `router.push('/dashboard/resumes')`）、简历名称、状态徽章（`同步中` / `已保存`）；右侧：左右栏 toggle、导出、保存；icon-only 按钮统一 `Tooltip`；不使用 `Separator`（其 `align-self: stretch` 会强制撑满行高），改为内部 `InlineDivider` 组件（`bg-default-200 h-5 w-px self-center`）。顶栏去掉底边框，由内容区 `bg-default-50` 与顶栏 `bg-background` 自然区分层次。
- **`_components/left-panel.tsx`**：新建。`AI 对话` 占位区，去掉 header 的底边框。
- **`_components/center-panel.tsx`**：新建。A4 比例白纸卡片 + 个人信息模块占位区；背景色和圆角由外层 `<main>` 负责，自身只承担内容布局。
- **`_components/right-panel.tsx`**：新建。HeroUI `Tabs`（默认 primary 变体）「模块管理 / 设计 / AI 诊断」，`Tabs.ListContainer` 加 `mt-2` 与面板顶拉开；不再手动加底边框，沿用 HeroUI 自带的 `Tabs.Indicator`。
- **`_components/resize-handle.tsx`**：新建。垂直方向拖拽手柄：1px 视觉线 + 左右各 4px 透明热区；`onPointerDown` 记录起点、`setPointerCapture` 接管后续事件；通过 `computeWidth(dx, base)` 让父级决定左/右栏方向；`aria-orientation="vertical"` + `role="separator"` + `aria-valuenow/min/max`；键盘 `←/→` 细调（默认 16px 步长），`Home/End` 贴最小/最大值；拖拽中加 `data-dragging` 切换 accent 色。
