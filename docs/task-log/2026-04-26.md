# 2026-04-26 Task Log

## 04:38

### Cursor Prompt

1. 按 `docs/api-docs-json.json` 中更新的 AI 对话相关接口，按 `apis-layer` 规范同步类型与 `apis`。
2. 将后台给出的 `/api/v1/ai/chat/stream` 调用约定纳入项目（类型、解析封装、`agent-context` 说明）。
3. 好请记录这些修改并提交

### 修改内容总结

**OpenAPI 与侧栏**

- `docs/api-docs-json.json`：已包含 AI 模块路径（对话 CRUD、消息列表、全局上下文、chat send/stream 等）及相应 schema 更新。
- `src/app/dashboard/layout.tsx`：侧栏「模板管理」文案改为「模块管理」。

**请求层**

- `src/lib/request.ts`：新增 `postSse`（同鉴权、不解析 body，供流式端点）。

**`src/types/business`**

- `ai-conversation.ts`：`TAiConversation` / `TAiMessage` / `TAiToolCall`、用途与角色枚举等。
- `ai-global-context.ts`：`TAiGlobalContext`。
- `ai-stream.ts`：流式帧内 JSON `TAiChatStreamEvent`（对应后端 `AiChatStreamEventDto`）。

**`src/types/api/ai/`**

- 各接口 Req：`conversation-*`、`message-list`、`global-context-*`、`chat-send`（`TSendAiChatReq` / `TSendAiChatRes`）；列表类含 `*ListRes` 与分页。

**`src/apis/ai.ts`**

- 薄封装：对话/消息/全局上下文的 `post` 与 `sendChat`；`streamChat` 返回 `Response`；`removeConversation` / `removeGlobalContext` 回避保留字 `delete`。

**`src/lib/ai/stream-chat.ts`**

- `parseSseChunk`、`streamAiChat`：与 `writeSse` 分帧一致，相对路径 `/api/v1/ai/chat/stream`，`Accept: text/event-stream`，鉴权可显式 `accessToken` 或走 storage。

**`agent-context/AI对话流式接口.md`**

- SSE 帧与 `phase` 说明、与仓库内类型/文件路径的对应，便于 @ 引用。

**本记录**

- `docs/task-log/2026-04-26.md`：本条目。

**未纳入版本库**

- `assets/` 下未跟踪的 png 等素材未提交，避免无关二进制进入仓库。

## 18:33

### Cursor Prompt

我调整了一下代码内容，请记录本次修改并提交

### 修改内容总结

**构建器左栏 · AI 对话与多轮会话**

- `src/lib/builder-resume-keys.ts`：新增 `resumeAiConversationsQueryKey`，供简历下 AI 会话列表与失效使用。
- `src/app/builder/[id]/_components/resume-ai-conversation-context.tsx`：`ResumeAiConversationProvider` + `useResumeAiConversation`；基于 `ai/conversation` 的拉列表、建线程、重命名、删除、当前选中与 `refetchConversations`；创建用途为 `DIALOGUE_EDIT`。
- `src/app/builder/[id]/page.tsx`：向 `LeftPanel` 传入 `resumeId`。
- `src/app/builder/[id]/_components/left-panel.tsx`：左栏为对话区布局（动态标题、加号新会话、时钟打开对话记录 Popover、按「今天/昨天/一周内/一周前」分组、消息区与输入区占位；输入区左下为 HeroUI `Select` 的 Ask/Agent 与发送按钮占位等）；经用户本地微调后的当前版本。

**本记录**

- `docs/task-log/2026-04-26.md`：本条目。

**未纳入本次提交**

- `assets/`、未纳入的新文件 `docs/ai-design.md` 仍不放入版本库（与上条「未纳入」策略一致，除非后续单独说明）。

## 21:40

### Cursor Prompt

记录这些修改并提交

### 修改内容总结

**左栏 AI 对话（流式、历史、展示与布局）**

- `left-panel.tsx`：发送消息并调用 `streamAiChat` 流式合并 `deltaText`；按会话缓存消息、切换 `selectedId` 时 `listMessages` 拉历史并做竞态防护；创建对话后同步 `setSelectedId`；左右气泡样式已按「用户 / 助手」预期区分；历史加载中禁用输入与发送；消息区用 flex 定高 + `overflow-y-auto` 实现独立滚动；对话正文使用下方「Markdown 渲染」组件展示。
- `page.tsx`：左栏 `aside` 增加 `h-full min-h-0 flex flex-col overflow-hidden`，保证与 `LeftPanel` 高度链一致、便于消息区滚动。

**类型与文案解析**

- `types/business/ai-conversation.ts`：`TAiMessage.text` 与后端 DTO 对齐为 `string | null`；`contentJson` / `providerMeta` 为 `unknown`。
- `lib/ai/ai-message-text.ts`：`getAiMessageDisplayText` 以字符串 `text` 为主，兼容历史 object 与 `contentJson` 中 ProseMirror 结构；`toChatLinesFromServerMessages` 转气泡列表用。

**Markdown 渲染（依赖与组件）**

- 新增依赖：`react-markdown`、`remark-gfm`、`rehype-sanitize`。
- `src/components/ai-chat-markdown.tsx`：GitHub 风味 Markdown、默认 schema 上扩展 table 系标签的 `rehype-sanitize`、`skipHtml` 与定制 `components` 的紧凑列表/表格/链接/代码等样式；外链新窗口与 `rel="noopener noreferrer"`。

**本记录**

- `docs/task-log/2026-04-26.md`：本条目。

**未纳入本次提交**

- `assets/`、未跟踪的 `docs/ai-design.md` 不纳入（与上条「未纳入」策略一致）。

## 21:45

### Cursor Prompt

1. 在构建器左栏聊天框增加「联网搜索」按钮，对应 `TSendAiChatReq.enableWebSearch`。
2. 记录这些修改并提交。

### 修改内容总结

- `src/app/builder/[id]/_components/left-panel.tsx`：新增 `enableWebSearch` 状态；`streamAiChat` 请求体在开启时传 `enableWebSearch: true`（关闭时省略字段）；左下角在 Ask/Agent 选择器旁增加可切换的「联网搜索」按钮（Globe 图标），流式或历史加载中禁用。

**本记录**

- `docs/task-log/2026-04-26.md`：本条目。

**未纳入本次提交**

- `assets/`、`docs/ai-design.md` 仍不纳入。

## 22:21

### Cursor Prompt

- 新对话：点击不立即建会话，首条发消息再建；流式/列表/选中与标题由后端等若干轮迭代修复。
- 新建对话后重新拉对话列表、修复新建后对话记录与选中错位、流式不展示、好请记录这些修改并提交。

### 修改内容总结

**`resume-ai-conversation-context.tsx`**

- `pendingNewThread` + `startNewThreadDraft`：新对话为草稿时勿自动将 `selectedId` 设回列表首条。
- `setSelectedId`：选中真实 id 时清除 `pendingNewThread`。
- 列表与选中校正 `useEffect`：拆分为「`selectedId == null` → 选首条」与「`selectedId` 不在列表中」；后者在 `isFetching` 为真时不回退，避免新会话 id 尚未进缓存时错选上一条。

**`left-panel.tsx`**

- 加号走 `startNewThreadDraft`：清空输入与未保存行，头显「新对话」；不调用 `createConversation`。
- 首条发消息：不传 `conversationId` 的流式；`meta` 中解析 `conversationId`，`setQueryData` 乐观插入占位会话、同步 `refetch`；`skipNextHistoryLoadForConvIdRef`：本次选中会触发 `listMessages` 时跳过一帧，避免用空/不完整历史覆盖流式中本地 `messagesByConv`。
- 未落 id 前用 `unsavedThreadLines`，落 id 后写入 `messagesByConv` 并处理 `meta` 前后 delta 的合并。
- 辅助方法：`tryParseConversationIdFromStreamPayload`、`upsertStreamCreatedConversationInCache` 等。

**本记录**

- `docs/task-log/2026-04-26.md`：本条目。

**未纳入本次提交**

- `assets/`、`docs/ai-design.md` 仍不纳入（与上条策略一致）。
