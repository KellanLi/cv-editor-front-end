# 2026-04-18 Task Log

## 04:37

### Cursor Prompt

在模块创建/编辑弹窗中实现信息层列表：`Surface` 为容器；从 `InfoLayerModal` 的 `onOk` 追加信息层；列表项展示顺序号、信息层类型名称、与 `const.ts` 中 `labels` 数量一致的字段名输入；数据对齐 `TInfoTemplate`。修复受控弹窗下「创建」无法打开的问题。拖拽改用 `@dnd-kit`；后改为仅左侧手柄可拖，避免输入框内拖选触发排序；新建项不预填、`placeholder` 为「请输入」、Label 在原名后加「字段名称」。按约定写入 task-log 并提交 git。

### 修改内容总结

- **`package.json` / `pnpm-lock.yaml`**：新增 `@dnd-kit/core`、`@dnd-kit/sortable`、`@dnd-kit/utilities`。
- **`section-builder.tsx`**：`'use client'`；`infoLayers` 状态与 `InfoLayerModal.onOk` 追加行；`Surface` 使用 `rounded-2xl` 包裹 `InfoLayerFieldList`；`Modal.Backdrop` 受控 `useOverlayState`；`ref.open` 支持 `TContentTemplateItem | null` 并同步 `infoTemplates`；「创建」在清空表单后调用 `overlayState.open()`。
- **`info-layer-field-list.tsx`**（新文件）：`TInfoTemplate` + `keyId` 行数据；`createInfoLayerRow` 按类型字段个数生成空 `names`；`DndContext` + `SortableContext` + `DragOverlay`；`listeners` 仅绑在 `GripVertical` 手柄；`PointerSensor` 位移阈值；Label 为「原名 + 字段名称」、`Input` `placeholder="请输入"`；`onDragEnd` 用 `arrayMove` 更新 `order`。
