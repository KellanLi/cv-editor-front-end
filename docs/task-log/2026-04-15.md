# 2026-04-15 Task Log

## 03:34

### Cursor Prompt

优化信息层选择弹窗：自定义宽度（不用 `size`）、用 HeroUI `CheckboxGroup` 管理选择与确认回调；按官方 Features and Add-ons 示例修正选中背景与默认边框；依据 task-log 技能记录并提交 git。

### 修改内容总结

- **`info-layer-modal.tsx`**：`Modal.Dialog` 用 `className` 控制宽度（`w-full max-w-4xl`），`Modal.Container` 不再用 `size` 控宽；`CheckboxGroup` 受控 `value`/`onChange`，单选逻辑；底部「确认」未选禁用，选中后 `onOk(INFO_LAYER)`；选项用 `Checkbox` + `variant="secondary"`，对齐文档的 `bg-surface`、`border-foreground/10`、选中态 `data-[selected=true]` / `aria-[checked=true]` + `!bg-accent/10`。
- **`section-builder.tsx`**：内联「添加信息层」弹窗抽离为 `InfoLayerModal`，侧栏仅保留 `<InfoLayerModal onOk={() => {}} />`。
